class Cachequery < ActiveRecord::Base
	self.table_name =  "domain_caches"

	# validates :name, presence: true, length: { minimum: 1 }
	# validates :value, presence: true, length: { minimum: 1 }
	# validates :expires_in, presence: true








end
